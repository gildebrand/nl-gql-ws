type Query {
  getUsers: [User!]!
  getUser(id: Int!): User!
  getCurrentUser: User!
}

type User {
  id: Int!
  email: String!
  name: String!
  avatarUrl: String
  createdTodos: [Todo!]!
  assignedTodos: [Todo!]!
}
