type Query {
  getTodos(filter: GetTodosFilter! = {}): [Todo!]!
}

input GetTodosFilter {
  done: Boolean
  authorId: Int
}

type Todo {
  id: Int!
  done: Boolean!
  title: String!
  author: User!
}
